#include <iostream>
#include <ostream>
#include <istream>
#include <fstream>

using namespace std;


cin.get(char c);
cout.put(char c);

istream
    read(char* s, int n);
    
    get(char& c);
    get(char* s, int n);
    
    getline(char* s, int n);
    getline(char* s, int n, char delim);

    
